Update the current PR description and, when necessary, its title from the final diff. Read `.claude/CLAUDE.md` and `commitlint.config.ts` first. If the user asks for a draft, return the proposed text without publishing it.

1. Identify the PR with `gh pr view --json number,url,title,body,baseRefName,headRefName,headRefOid`. Check that the checkout and pushed head match this PR; exclude uncommitted or unpushed work from claims about the PR. If no PR exists, draft the summary and say so.
2. Fetch the PR's actual base and review the complete diff and commits from its merge base to the PR head. Normal PRs target `main`; for an existing stacked PR, use its recorded base without retargeting it. Read the changed files, not just `git diff --stat`, and distinguish this PR's work from its dependencies.
3. Collect validation appropriate to the changes, following `.claude/CLAUDE.md`. For Python changes, use the project's selected interpreter with `-m pytest`; Blender checks must identify the version and the build tested. Capture each command's real exit status before truncating its output. Reuse current evidence when it covers the final diff. Documentation-only changes do not require the entire Blender suite. Do not run paid Scenario operations merely to populate a test count.
4. Write a concise body: the concrete problem and resulting behavior, the changes a reviewer needs to understand, and validation with commands/results and relevant limitations. Preserve useful issue links and mark tasks checked only when actually verified. Include counts only when measured; use stats only when they help the review. For API changes, confirm the SDK-first rule and link the demonstrated SDK gap for any raw API exception. Do not expose private research or credentials in this public repository.
5. Pick a Conventional Commit title from the final diff and the allowed scopes in `commitlint.config.ts`: test-only work is `test`, documentation and agent command Markdown is `docs`, and product changes use the appropriate product type. Follow the 120-character limit and no-em-dash rule. Lint a changed title with the repository configuration.
6. Write the body to a temporary file with real newlines, then publish with `gh pr edit <number> --body-file <file>` and, if needed, `--title <title>`. Pass title and paths as properly quoted arguments. Updating this summary does not commit, push or merge code.
7. Fetch the edited PR to verify its title/body, then report the URL and any outstanding validation.
